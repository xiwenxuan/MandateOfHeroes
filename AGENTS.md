# MandateOfHeroes 项目规则

## 1. 权威与入口

规则优先级如下：

```text
用户当轮明确指令
→ 本文件
→ .codex/skills/mandate-unity-development/SKILL.md
→ Skill 按需参考资料
→ 当前任务直接相关的 Docs 设计与任务书
→ 现有代码、测试和存档兼容事实
```

- 涉及本项目 Unity/C#、领域模型、世界模拟、内容数据、存档、测试或里程碑时，
  必须使用 `mandate-unity-development`。
- 当前会话未自动发现该 Skill 时，必须手动完整读取其 `SKILL.md`，不得跳过。
- Skill 只规定执行方法，不能扩大授权、弱化本文件或替代用户决策。
- 跨系统现状、全局建设顺序，以及生产、建设和科研规则以
  `Docs/GAME_SYSTEMS_MASTER_AND_STATUS.md` 为统一入口。
- “已有原型、已有底座、已定方案、待研究”必须按总纲定义使用；没有代码、
  可操作原型或验证证据时，不得把设计目标写成已经实现。
- 总纲负责当前状态和跨系统关系；各核心设计文档负责本领域细则。两者出现
  实质冲突时，不得静默选择，必须指出冲突并请求确认。

## 2. 开工与范围

- 先检查 `git status`、相关设计、现有实现和测试。
- 保留用户已有和无关修改；只修改当前任务范围内的文件。
- 回答、审阅和诊断默认只读；只有用户要求实现、修复或建设时才修改。
- 会改变存档、架构、公开行为、开发顺序或外部状态的假设必须明确说明。
- 未经授权不得提交、推送、创建 PR、删除数据或关闭用户程序。

## 3. 不可违反的领域规则

- 核心世界事实不得只存在于 UI、场景对象或表现层。
- 随机行为使用稳定 ID、明确时间/动作坐标和项目确定性随机机制。
- 存档是兼容合同；持久结构变化必须顺序迁移、往返测试并校验不变量。
- 人口、兵员、物资和财政变化必须可追溯，不能重复计算或凭空产生。
- 每个人从出生起具有永久身份；汇总缓存可以降低计算成本，但不得删除、
  合并、替代或重新随机永久人物。
- 人口、家户、AI 调度、关注演出或大规模存储任务必须读取
  `Docs/TASK_M12_PERMANENT_POPULATION_AND_ATTENTION.md`。
- 玩家关注只改变信息和表现精度，不得改变同一世界事实。
- 外部代码、数据和素材必须具有兼容许可证并记录来源；不得复制商业游戏资产。
- 作物、产品、配方、设施、技艺、词条、科技、装备、兵种、任务等可持续扩展的
  开放内容集合必须优先使用稳定命名空间ID和数据定义；只有生命周期、协议状态等
  真正封闭集合才使用枚举。增加普通内容不得要求升级存档结构；缺失内容ID不得
  静默改指、删除或重新随机，必须保留原引用并明确报告或迁移。

## 4. 架构边界

- `Mandate.Domain`：纯 C# 状态、不变量、稳定身份和确定性规则。
- `Mandate.Simulation`：跨实体、时间推进和世界系统编排。
- `Mandate.Persistence`：序列化、存档版本和迁移。
- `Mandate.Presentation`：Unity 界面、输入、场景和表现。
- 不得让 Domain 反向依赖 Unity 表现层。

详细目录和设计文档路由由 Skill 参考资料维护，不在本文件重复。

## 5. 验证规则

代码任务按以下顺序验收：

```text
全工程编译
→ 核心测试
→ 受控 Unity 测试
→ git diff --check
→ 差异审阅
```

- 优先使用 Skill 的 `scripts/verify-project.ps1`。
- 纯文档任务使用 `-DocumentationOnly`，不要求编译或 Unity。
- `-SkipUnity` 只允许用于明确无需 Unity 的任务或已证实的环境阻塞，并记录原因。
- 没有结果文件或明确汇总，不得声称测试通过。
- 编译、核心测试和 Unity 测试必须分别报告，不能互相替代。

## 6. 防卡死硬规则

- 禁止无边界地以前台方式运行 Unity、构建器、测试器或其他长驻进程。
- 外部工具默认硬超时 300 秒；延长前必须取得用户同意。
- 长任务在后台启动，每隔不超过 5 秒轮询，并保留 PID、日志和结果路径。
- 超时后只终止本任务启动的进程树，并报告阶段与日志尾部。
- 启动 Unity 批处理前检查现有 Unity 进程；编辑器已打开时报告锁冲突，
  不得擅自关闭。
- 外层调用中断后，下一轮先检查并清理本任务启动且已经超时的遗留进程。
- Unity 测试只能通过 `Tools/Run-UnityTestsSafe.ps1`。

## 7. 版本与维护

- 提交前至少完成适用的验证、`git diff --check` 和范围审阅。
- 只暂存当前任务文件，并在提交后报告提交号和剩余工作区修改。
- `AGENTS.md`、程序集、Unity 版本、存档版本或测试入口变化时，同步复核 Skill。
- 领域设计发生方向变化时，同时更新总设计、任务路由和旧任务书的兼容说明。
- 全局开发顺序变化时更新系统总纲；领域文档中的 P1、P2 或“阶段”只表示
  该领域内部依赖，除非总纲明确将其列入当前顺序。
