# 任务书：通用项目聊天归档与跨聊天接管协议 V1

## 一、任务定位

### 1.1 任务名称

通用项目聊天归档与跨聊天接管协议 V1

### 1.2 适用范围

本任务书适用于所有开发、写作、研究、数据整理及其他长期项目。

同一份任务书同时适用于：

1. 即将停止使用的旧聊天框；
2. 准备接管项目的新聊天框。

用户粘贴本任务书后，会明确发送以下指令之一：

```text
执行旧聊天框部分
```

或者：

```text
执行新聊天框部分
```

只能执行用户指定的部分，不得同时执行两个部分。

如果用户尚未指定，应等待用户指定，不得自行判断。

---

## 二、任务目标

建立一个不依赖聊天框历史加载状态的项目交接机制，实现：

1. 将旧聊天框中的重要聊天记录整理成项目文档；
2. 保存用户已经确认的决策、需求和否决意见；
3. 保存当前项目状态、文件修改、测试结果和下一步计划；
4. 让新聊天框通过项目文件恢复上下文；
5. 避免新聊天框重新读取全部超长聊天记录；
6. 防止新旧聊天框同时修改项目；
7. 保留可追溯的历史档案；
8. 不删除原聊天框，不删除原始会话数据。

---

## 三、统一目录

在项目根目录使用以下结构：

```text
.codex/
└─ handoff/
   ├─ CURRENT.md
   ├─ INDEX.md
   ├─ history/
   │  └─ YYYY-MM-DD_HH-mm_项目名_聊天标题.md
   └─ assets/
      └─ 交接编号/
```

文件用途：

- `CURRENT.md`：新聊天框默认读取的精简交接单；
- `INDEX.md`：历次交接档案索引；
- `history/*.md`：旧聊天框形成的结构化聊天档案；
- `assets/`：需要长期保留的任务书、图片或附件。

如果项目没有 `.codex` 目录，可以创建。

如果当前工作环境不允许创建该目录，应明确报告，不得声称已经生成交接文件。

---

# 四、旧聊天框执行部分

仅当用户发送：

```text
执行旧聊天框部分
```

时执行本章。

## 4.1 项目识别

首先确认：

- 项目名称；
- 项目根目录；
- 当前工作目录；
- 是否为 Git 项目；
- 当前分支和 HEAD；
- 项目规则文件；
- 当前任务书；
- 当前开发目标。

优先读取：

1. 项目根目录的 `AGENTS.md`；
2. 项目自身的总纲、README、路线图或状态文档；
3. 当前任务书；
4. 当前实现和测试；
5. Git 工作区状态。

不得根据聊天记忆直接声称项目状态，必须与文件系统核对。

## 4.2 生成聊天历史档案

生成：

```text
.codex/handoff/history/YYYY-MM-DD_HH-mm_项目名_聊天标题.md
```

聊天档案必须包含：

### A. 会话信息

- 项目名称；
- 项目目录；
- 旧聊天框标题；
- 可获得时记录聊天框 ID；
- 归档时间；
- 交接编号；
- 当前分支和提交号；
- 档案完整性状态。

### B. 用户需求记录

按照时间和主题整理：

- 用户提出的主要需求；
- 用户确认接受的方案；
- 用户否决的方案；
- 用户修改过的方向；
- 用户明确授权的操作；
- 用户明确禁止的操作；
- 尚未解决的问题。

关键决策尽量保留用户原话，不得改变原意。

### C. AI 执行记录

记录：

- 已完成的任务；
- 修改或创建的文件；
- 采用的设计和架构；
- 执行过的测试；
- 编译、测试、验收结果；
- 提交和推送状态；
- 遇到的问题及处理结果；
- 尚未完成的工作。

不复制大量无意义终端输出。终端和测试输出只保留：

- 执行命令；
- 结果摘要；
- 错误信息；
- 证据文件路径。

### D. 附件和任务书

对聊天中涉及的关键附件：

- 已经位于项目中的文件，记录项目相对路径；
- 位于临时目录且对后续开发必要的附件，在权限允许时复制到 `assets/交接编号/`；
- 无法复制的附件，记录原路径和不可持续风险；
- 不复制密码、令牌、个人隐私或无关文件。

### E. 档案完整性

如果旧聊天经过上下文压缩，或者无法访问部分早期记录，必须写明：

```text
本档案不是逐字完整导出；缺失部分保留在原聊天框中。
```

不得凭记忆补造缺失内容。

## 4.3 生成 CURRENT 接管单

更新：

```text
.codex/handoff/CURRENT.md
```

写入以下结构：

```markdown
# 当前项目交接单

## 元数据

- 交接编号：
- 生成时间：
- 项目名称：
- 项目根目录：
- 来源聊天框：
- 来源聊天框 ID：
- 接管状态：READY
- 历史档案：

## 当前目标

说明项目当前真正要完成的结果。

## 当前任务

说明正在执行的任务书、当前阶段和当前范围。

## 用户已确认决策

只记录已经明确接受的决定。

## 用户否决或废弃内容

记录不得重新采用的旧方案、旧数据和旧方向。

## 已完成内容

列出已经有实现、文件或验证证据的成果。

## 正在进行的内容

列出尚未收口、正在修改或等待验证的工作。

## 尚未完成内容

列出剩余任务、缺口和验收项。

## 项目实时状态

- Git 分支：
- HEAD：
- 工作区是否干净：
- 已修改文件：
- 未跟踪文件：
- 用户原有修改：
- 本任务修改：
- 未提交内容：
- 未推送内容：

## 验证状态

分别记录：

- 编译：
- 核心测试：
- 编辑器测试：
- PlayMode 测试：
- 人工验收：
- 未执行项目及原因：

不得用一种测试代替另一种测试。

## 当前阻塞

记录环境、权限、外部服务、人工选择等阻塞。

## 下一步动作

给出新聊天框接管后可以直接执行的第一项具体工作。

## 必读文件

列出新聊天框必须读取的规则、总纲、任务书、设计、代码和测试。

## 禁止事项

列出不得删除、覆盖、回滚、重复实现或擅自修改的内容。
```

## 4.4 更新索引

在 `.codex/handoff/INDEX.md` 中追加本次交接记录：

- 交接编号；
- 时间；
- 项目名称；
- 来源聊天框；
- 历史档案路径；
- CURRENT 版本；
- 状态 `READY`。

不得覆盖以前的索引记录。

## 4.5 旧聊天框停止条件

完成文件生成后：

1. 检查交接文档是否能够正常读取；
2. 检查文档中的路径是否存在；
3. 检查 Git 状态是否准确；
4. 报告生成的文件；
5. 告知用户可以创建新聊天框。

旧聊天框此时不得：

- 删除聊天记录；
- 删除本地会话文件；
- 自动归档自身；
- 自动提交或推送交接文件；
- 继续执行新的项目开发；
- 假装新聊天框已经接管。

旧聊天框应等待新聊天框确认接管成功。

---

# 五、新聊天框执行部分

仅当用户发送：

```text
执行新聊天框部分
```

时执行本章。

## 5.1 定位项目

确认项目根目录。

如果用户已经从保存的项目打开聊天框，使用当前项目目录。

如果当前目录不明确，只询问一次项目路径，不得猜测其他项目。

## 5.2 读取顺序

按照以下顺序读取：

1. 项目根目录的 `AGENTS.md`；
2. `.codex/handoff/CURRENT.md`；
3. CURRENT 中列出的项目总纲；
4. CURRENT 中列出的当前任务书；
5. CURRENT 中列出的关键代码和测试；
6. 必要时读取 `.codex/handoff/INDEX.md`；
7. 只有发生决策争议时，才读取对应的 `history/*.md`。

不得默认读取所有历史档案。

## 5.3 实时核验

将交接单与当前项目状态对比：

- 项目目录；
- Git 分支；
- HEAD；
- 已修改文件；
- 未跟踪文件；
- 当前任务书；
- 关键实现文件；
- 测试和证据文件；
- 外部工具状态。

如果实时状态与交接单不同：

1. 保留实时文件；
2. 不回滚任何修改；
3. 列出差异；
4. 判断差异是否影响当前任务；
5. 必要时请求用户确认。

不得为了匹配交接单而恢复、删除或覆盖文件。

## 5.4 接管确认

核验完成后，将 `.codex/handoff/CURRENT.md` 中的状态更新为：

```text
CLAIMED
```

并补充：

- 接管时间；
- 新聊天框标题；
- 可获得时记录新聊天框 ID；
- 核验结果；
- 发现的状态差异。

同时在 `INDEX.md` 更新本次交接状态。

## 5.5 继续当前任务

接管后：

1. 简要汇报接管结果；
2. 明确当前目标；
3. 明确第一项继续动作；
4. 按当前任务书继续执行；
5. 遵守项目规则和验证要求；
6. 不要求用户重新讲述已经写入交接单的内容。

如果 CURRENT 只要求接管而没有授权继续修改，则完成核验后等待用户。

如果 CURRENT 明确记录了正在执行且已授权的任务，则从下一步继续。

---

# 六、通用安全规则

无论执行旧部分还是新部分，都必须遵守：

1. 不删除原聊天框和原始会话文件；
2. 不擅自关闭用户程序；
3. 不自动提交、推送或创建 PR；
4. 不覆盖用户已有修改；
5. 不把未验证目标描述成已完成；
6. 不把聊天记忆当成项目实时状态；
7. 不复制密码、令牌或隐私数据；
8. 不让两个聊天框同时修改同一项目；
9. 不因交接而改变项目架构、存档或公开行为；
10. 所有无法确认的信息必须标记为未知或待核验。

---

# 七、验收标准

## 7.1 旧聊天框验收

满足以下条件才算完成：

- 历史档案已生成；
- CURRENT 已生成；
- INDEX 已更新；
- 关键用户决策已记录；
- Git 和测试状态已记录；
- 下一步动作明确；
- 文件路径可访问；
- 未删除或覆盖旧记录；
- 状态为 `READY`。

## 7.2 新聊天框验收

满足以下条件才算完成：

- 已读取项目规则；
- 已读取 CURRENT；
- 已核验实时项目状态；
- 已报告差异；
- 已确认当前任务和下一步；
- 状态更新为 `CLAIMED`；
- 已开始或准备继续当前任务；
- 没有重新加载无关的全部历史。

---

# 八、用户使用方式

## 8.1 旧聊天框

先粘贴本任务书，再发送：

```text
执行旧聊天框部分
```

## 8.2 新聊天框

打开同一个项目，粘贴同一份任务书，再发送：

```text
执行新聊天框部分
```

如果新聊天框未绑定项目，再补充：

```text
项目目录：项目的绝对路径
执行新聊天框部分
```
