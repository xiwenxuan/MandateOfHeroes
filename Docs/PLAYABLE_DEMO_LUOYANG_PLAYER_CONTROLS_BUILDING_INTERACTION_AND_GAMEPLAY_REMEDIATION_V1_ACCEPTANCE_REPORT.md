# PlayableDemo 洛阳玩家镜头、建筑交互与首条玩法闭环整改 V1 验收报告

## 1. 当前结论

`IMPLEMENTED / CORE VERIFIED / LIVE EDITOR VERIFIED / AUTOMATED PLAYMODE RE-RUN BLOCKED BY OPEN EDITOR`

2026-09-01 的人工反馈有效：上一版只证明正式人物可移动，没有达到可玩游戏的操作与反馈标准。
本轮已按五项反馈完成代码整改，并已在用户当前打开的 Unity Game View 完成主路径人工操作。
Unity 编辑器 PID `21736` 仍正由用户使用，因此安全入口拒绝再启动第二个编辑器；自动 PlayMode
尚无结果文件，不能据此标记最终 `ACCEPTED`。

## 2. 已完成

- 中键拖动平移，右键拖动旋转，WASD、滚轮和 `F` 跟随继续兼容；
- 右键不再同时发出移动命令，避免输入冲突；
- 建筑信息卡显示正式开局 Facility 名称、分类、状态、完好度、历史依据、Cell 和 Access；
- 本地镜头设为 8，人物表现放大为 2.4 倍并显示“◆我/◆我·行走中”；
- 目标卡和“前往此处”把正式移动过程显式呈现，24 单位/秒仅压缩 Unity 回放时间，不改变正式移动结算；
- 战略地形与人物尺度场景分层，本地建筑、人物、路线统一到同一 Floating Origin；
- 模块化建筑在人物尺度下使用 X/Z 占地适配、受控 Y 高度，关闭普通玩家画面的红色 Footprint 调试面；
- 洛阳市集交易与市场市曹本地差事接入既有 PlayerAction、交易和任务账；
- 不增加第二套人物、建筑、路线、钱包、库存、任务或存档版本。

## 3. 自动证据

- 全工程编译：`PASS`；
- 目标核心测试：`4/4 PASS`；
- `git diff --check`：`PASS`；
- 目标 PlayMode：`BLOCKED BY OPEN EDITOR PID 21736`，未关闭用户程序。

## 4. Game View 人工证据

- 直接进入“洛阳·人物近景”，不是旧战略示意图；
- 选择正式“市场”后，建筑卡显示市集类型、正常运作、100% 完好度、历史/空间依据、Cell、Access 与人物位置；
- 触发 `E` 后，顶部状态变成“行走中”，世界标记变成“◆我·行走中”，黄色路线和人物位置连续更新；
- 约 10 秒后状态变为“市场 / 可行动”，建筑卡显示“人物位置：已到达”；
- 到达后出现“接受本地任务 / 买入匹布 / 卖出随身布料”；
- 实际点击买入后，钱财 `200 → 40`、世界 `第1日 → 第2日`，日志为“买入2单位布帛，支出160钱”，
  当前目标切换为市场市曹差事；
- Unity Console 显示 0 Error、0 Warning。

## 5. 待验收

1. 关闭 Unity 编辑器后运行目标 PlayMode；
2. 自动确认中键平移、右键旋转、建筑选择、移动结束与交易断言并生成正式结果文件；
3. 再执行市曹接差/推进，并纳入后续 20—30 分钟独立人类盲玩；
4. 自动 PlayMode 和独立盲玩门禁闭合前，不把本任务或 M26 标记为最终接受。
