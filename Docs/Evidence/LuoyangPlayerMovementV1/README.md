# 洛阳正式玩家人物移动 V1 证据索引

本目录保存 `TASK_LUOYANG_FORMAL_PLAYER_MOVEMENT_WORLD_SETTLEMENT_V1.md` 的稳定文字证据。原始机器日志位于
仓库忽略的 `tmp/skill-verification` 与 `tmp/unity-validation`；本目录只记录可复核的命令范围、结果文件、
哈希和最终提交，不提交 Unity 临时目录。

- `test-results.txt`：编译、定向测试和完整核心分组回归。
- `unity-validation.txt`：ProjectLoad、EditMode 与 PlayMode 受控验收。
- `replay-results.txt`：三次相同初始状态和命令序列的状态哈希。
- `git-diff-summary.txt`：最终范围、差异检查、提交和推送状态。

证据在全部门禁完成后一次性冻结；最终判断只看
`Docs/LUOYANG_FORMAL_PLAYER_MOVEMENT_V1_ACCEPTANCE_REPORT.md`。
