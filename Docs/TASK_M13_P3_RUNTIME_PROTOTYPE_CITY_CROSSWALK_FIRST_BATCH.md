# M13-P3任务书：运行时—184原型—77城交叉映射第一批

## 一、任务定位

本任务继续执行
[`TASK_M13_HAN_140_POPULATION_AND_STABLE_GEOGRAPHY.md`](TASK_M13_HAN_140_POPULATION_AND_STABLE_GEOGRAPHY.md)
的P3阶段，先完成当前可运行河北走廊的最小闭环，不一次性展开约1180县或77城全表。

本批把三套游戏地点身份与140年行政截面、稳定地理身份分开保存：

```text
location.*  当前Unity运行时地点
L###        184年原型地图目录
C###        77城设计目录
admin.*     140年行政截面
geo.*       跨年代稳定地理身份
```

交叉映射只建立引用关系，不修改运行时公开ID，不把城市节点变成人口来源，也不把
同一郡国人口复制到多个县城或目录节点。

## 二、交付范围

### 2.1 140年县级候选

依据《后汉书》郡国志县目新增四个县级行政身份：

| 县级ID | 140年名称 | 上级 |
|---|---|---|
| `admin.han140.youzhou.zhuo.zhuo` | 涿 | 涿郡 |
| `admin.han140.jizhou.julu.xiaquyang` | 下曲阳 | 钜鹿郡 |
| `admin.han140.jizhou.julu.guangzong` | 广宗 | 钜鹿郡 |
| `admin.han140.jizhou.wei.ye` | 邺 | 魏郡 |

中山国节点和安平国节点继续保持区域代理，不在考证不足时强行改名为卢奴或信都。

### 2.2 稳定县级地理身份

在既有涿郡、钜鹿郡、魏郡临时地理桶下新增四个`county_area`：

- 涿县聚落与邻接县域；
- 下曲阳聚落与邻接县域；
- 广宗聚落与邻接县域；
- 邺县聚落与漳河邻接区域。

所有节点保持`geometry_status=provisional`、坐标留空。现代位置只作检索提示，
不表示140年县界、古河道或古城址范围已经精确复原。

### 2.3 游戏地点交叉

新增15条交叉映射：

| 类别 | ID |
|---|---|
| 运行时6项 | `location.zhuo`、`location.zhongshan`、`location.anping`、`location.xiaquyang`、`location.guangzong`、`location.ye` |
| 184原型6项 | `L001`、`L004`、`L006`、`L007`、`L008`、`L011` |
| 77城相关3项 | `C009`、`C010`、`C012` |

涿、下曲阳、广宗、邺按`approximate`映射到各自临时县级地理身份；中山、安平和
`C010`钜鹿按`aggregate`映射到既有郡国尺度地理区。映射状态描述空间与目录精度，
不改变人口证据等级。

## 三、来源与许可

- 《后汉书》卷三十郡国二列有魏郡之邺及钜鹿郡之下曲阳、广宗；
- 《后汉书》卷三十三郡国五列有涿郡之涿；
- 运行时地点来自项目代码`PrototypeWorldFactory.cs`；
- `L###`来自项目原创原型地图文档；
- `C###`来自项目原创77城并集目录。

古籍事实属于公共领域；项目代码和目录随仓库MIT许可证发布。本批不复制商业游戏
坐标、参数、地图、数据库、界面或素材。

## 四、明确不做

- 不给四个县级节点分配户口或新增人口记录；
- 不修改105条郡国人口映射及其10,000基点守恒关系；
- 不把下曲阳和广宗各自计入一份钜鹿郡人口；
- 不把中山国节点强制解释为卢奴县，也不把安平国节点强制解释为信都县；
- 不填充其余`L###`和`C###`；
- 不修改Unity场景、运行时地点ID、存档版本或永久人物；
- 不声明P3、县级全国目录或M13整体完成。

## 五、验收标准

1. 来源登记累计4项，行政单位累计123项；
2. 四个县级行政ID全部存在、上级正确、类型均为`county`；
3. 稳定地理累计158项，其中新增四个`county_area`且父级正确；
4. 交叉表累计15项，包含6个`runtime`、6个`prototype_catalog`和3个
   `city_catalog`；
5. 映射状态为9个`approximate`和6个`aggregate`，全部标记为临时；
6. 六个现有`location.*`完整覆盖且公开ID未修改；
7. `location.*`与对应`L###`共享相同稳定地理和行政身份；
8. `C009`与邺共享县级身份，`C010`保持钜鹿郡域代理，`C012`保持中山国代理；
9. 人口记录仍为105项、人口映射仍为105条、权重错误仍为0；
10. 专项数据验证、失败样例、确定性审计、全工程编译、核心回归和
    `git diff --check`通过。

## 六、完成记录

- 状态：已完成（2026-07-31）
- 存档影响：无
- Unity序列化影响：无
- 数据结果：
  - 来源4项、行政单位123项、稳定地理158项；
  - 新增4个县级行政候选和4个临时县级稳定地理身份；
  - 地点交叉15项，其中`runtime=6`、`prototype_catalog=6`、
    `city_catalog=3`；
  - 映射状态为`approximate=9`、`aggregate=6`；
  - 人口记录105项、人口映射105条、人口映射权重错误0。
- 验证结果：
  - `Validate-Han140PopulationData.ps1`通过：
    `sources=4 admin=123 population=105 regions=158 mappings=105 crosswalks=15`；
  - `Test-Han140PopulationValidator.ps1`通过：39/39；
  - 全工程编译通过；
  - 核心回归通过：104/104；
  - Unity测试未运行：本批仅修改离线JSON、CSV、文档和PowerShell数据测试，
    不涉及Unity场景、序列化或运行时程序集；
  - `git diff --check`通过。
- 下一阶段建议：继续P3第二批，优先补齐184年原型目录其余6项
  `L002/L003/L005/L009/L010/L012`，并将其中可考县级节点与蓟、广阳、
  钜鹿郡治和世界出口分别建模。
