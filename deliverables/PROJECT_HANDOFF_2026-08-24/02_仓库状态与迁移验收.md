# 仓库状态与迁移验收记录

## 1. 迁移事实

| 字段 | 数值 |
|---|---|
| 原项目 | `E:\project\gamedevelop\MandateOfHeroes` |
| 新项目 | `J:\project\MandateOfHeroes` |
| 迁移日期 | 2026-08-24 |
| 纳入文件数 | 12,480 |
| 纳入总字节 | 1,328,114,676 |
| 最终待复制文件 | 0 |
| 复制失败文件 | 0 |
| Git 对象连通性 | PASS |
| Unity 版本 | `2022.3.62f3c1` |

正式源码、文档、历史 Reference、Knowledge Base、Registry、Data、MapData、MapPipeline、Tools、
StreamingAssets、测试、场景、Shader、现有 Builds 和完整 `.git` 均已迁移。

## 2. Git 基线

```text
Remote:  https://github.com/xiwenxuan/MandateOfHeroes.git
Branch:  codex/m23-p4-quality-artisan-growth
HEAD:    191bca0bbe58f7f09eff91ef9254596953fb1ab6
Commit:  chore: finalize formal repository baseline
Tracking: origin/codex/m23-p4-quality-artisan-growth, ahead 3
```

迁移后源、目标的 HEAD、分支和完整 `git status --porcelain` 已逐条比较，结果一致。

## 3. 未提交工作区是正式开发状态的一部分

交接时预期：

```text
git status 条目：342
已跟踪但未暂存修改：32
已暂存修改：0
未跟踪文件/目录条目：310
```

未跟踪项按顶层目录分布：

| 顶层目录 | 条目数 |
|---|---:|
| `Docs` | 192 |
| `Assets` | 106 |
| `MapPipeline` | 12 |

这些内容包含全国空间基础、洛阳 T4、自然底图、Style D、运行时代码、测试、任务书、报告、
地图数据与生成工具。它们不是可以直接清理的垃圾。

在新电脑首次打开前后分别执行：

```powershell
git status -sb
git status --porcelain=v1 --untracked-files=all
```

如果条目数与上述基线不同，先调查 Unity 是否生成了新的可忽略文件、复制是否遗漏，或用户是否
已经继续修改；不要用清理命令强行把数字改回去。

## 4. 远端恢复风险

当前分支比对应远端分支领先 3 个提交，同时还有 342 条未提交状态。因此：

- 单独从 GitHub 克隆不能恢复当前项目；
- 只有这份完整迁移目录和旧 `E:` 目录包含本轮全部状态；
- 在完成正式提交范围审计、编译与测试之前，不要直接 `git add .`、`git add -A` 或推送；
- 不要切换分支覆盖当前工作树；如必须切换，先做可验证备份并取得用户同意。

## 5. 迁移排除项

仅排除了可重新生成或本机临时内容：Unity `Library/Temp/Logs`、编译 `obj/bin`、根目录
`tmp/outputs`、`.codex-docx-qa`、`*.user`、`*.suo`、`Thumbs.db` 和 `.DS_Store`。

现有 `Builds/` 被保留；正式文档内部的输出证据也被保留。

## 6. 备份保留建议

在新电脑完成下列事项前，保留旧 `E:` 项目和 `J:` 迁移盘：

1. Git 状态一致；
2. Unity 项目成功导入并编译；
3. PlayableDemo 和当前地图审图场景能打开；
4. 至少一次受控 ProjectLoadSmoke 和目标 EditMode 测试成功；
5. 重要未提交成果完成正式审计和提交。
